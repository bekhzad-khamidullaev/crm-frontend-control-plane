export const TELEPHONY_MODAL_MASK_STYLE = Object.freeze({
  backgroundColor: 'rgba(0, 0, 0, 0.32)',
  backdropFilter: 'blur(1px)',
});

export const TELEPHONY_MODAL_STYLES = Object.freeze({
  mask: TELEPHONY_MODAL_MASK_STYLE,
});

export const TELEPHONY_MODAL_PROPS = Object.freeze({
  centered: true,
  styles: TELEPHONY_MODAL_STYLES,
});

