/**
 * Company entity query keys factory
 * Centralized query key management for companies
 */

import { createQueryKeys } from '@/shared/api/query-client';

export const companyKeys = createQueryKeys('companies');
